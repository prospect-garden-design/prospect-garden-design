const {
  globalPrefix: prefix4g,
  compPrefix: prefix4c,
} = require('../../../utils/globalConfig');

module.exports = {
  toolbar: {
    'bg-color': { value: `{${prefix4g}.color.gray.light.val.value}` },
  },
};
